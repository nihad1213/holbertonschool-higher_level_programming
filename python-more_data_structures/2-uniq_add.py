#!/usr/bin/python3

def uniq_add(my_list=[]):
    new_list = set(my_list)
    new_new_list = list(new_list)
    
    if len(new_new_lsit) == 0:
        return 0
    sum = 0

    for i in range(0, len(new_new_list)):
        sum = sum + new_new_list[i]
        converted_sum = int(sum)

    return converted_sum
